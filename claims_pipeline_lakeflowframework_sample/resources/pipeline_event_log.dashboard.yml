resources:
  dashboards:
    pipeline_event_log:
      display_name: "Pipeline Event Log"
      file_path: ../src/dashboards/pipeline_event_log.lvdash.json
      warehouse_id: ${var.warehouse_id}
      dataset_catalog: ${var.catalog}
      dataset_schema: ${var.gold_schema}
