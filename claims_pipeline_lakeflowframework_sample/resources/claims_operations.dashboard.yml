resources:
  dashboards:
    claims_operations:
      display_name: "Claims Operations & Settlement"
      file_path: ../src/dashboards/claims_operations.lvdash.json
      warehouse_id: ${var.warehouse_id}
      dataset_catalog: ${var.catalog}
      dataset_schema: ${var.gold_schema}
