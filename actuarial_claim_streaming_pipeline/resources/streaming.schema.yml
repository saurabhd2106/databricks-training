# Unity Catalog schema for streaming bronze tables and the landing volume.

resources:
  schemas:
    actuarial_streaming:
      catalog_name: ${var.catalog}
      name: streaming
      comment: Actuarial streaming bronze (Auto Loader Streaming Tables) and landing volume.
