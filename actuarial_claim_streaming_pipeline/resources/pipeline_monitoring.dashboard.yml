resources:
  dashboards:
    pipeline_monitoring:
      display_name: "Pipeline Monitoring"
      file_path: ../src/dashboards/pipeline_monitoring.lvdash.json
      warehouse_id: ${var.warehouse_id}
      dataset_catalog: ${var.catalog}
      dataset_schema: ${resources.schemas.actuarial_streaming.name}
